[color]
	diff = auto
	status = auto
	branch = auto
	ui = auto
[rerere]
	enabled = true
[gist]
	private = yes
[diff]
	noprefix = on
[branch]
	autosetupmerge = always
    autosetuprebase = always
[alias]
	graph = log --format=damn-awesome --graph
    deploy = "!bash -c '[ -z $1 ] && echo Specify an environment 1>&2 && exit 1 || git push origin +HEAD:deploy-$1' -- "
[pretty]
	damn-awesome = %Cred%h%Creset -%C(yellow)%d%Creset %s %Cgreen(%cr) %C(bold blue)<%an>%Creset
[core]
	pager = less -r
	quotepath = false
	abbrev = 12
[log]
	date = local
[push]
	default = simple
	followTags = true
[mergetool]
	keepBackup = true
[merge]
	conflictstyle = diff3
[color "status"]
    added = yellow
    changed = green
    untracked = cyan
[color]
    branch = auto
    diff = auto
    status = auto
    ui = true
[user]
	name = Aria Stewart
    email = aredridel@dinhe.net
[filter "media"]
	required = true
	clean = git media clean %f
	smudge = git media smudge %f
[github]
	user = aredridel
[ghi]
	token = !security find-internet-password -a aredridel -s github.com -l 'ghi token' -w
[url "git@github.com:"]
	insteadOf = git://github.com/
[url "git@github.com:"]
	insteadOf = https://github.com/
[url "git@github.com:"]
	insteadOf = http://github.com/
[include]
    path = .gitconfig.local
